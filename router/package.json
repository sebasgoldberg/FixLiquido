{
  "name": "myapprouter",
  "scripts": {
    "preinstall": "cd resources/ui5/ && npm i --only=dev && npm i --only=prod && npm run build",
    "start": "nodemon node_modules/@sap/approuter/approuter.js"
  },
  "dependencies": {
    "@sap/approuter": "^6.0.1",
    "nodemon": "^2.0.2"
  }
}
